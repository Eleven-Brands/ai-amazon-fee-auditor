---
phase: 02-detection-output-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - run_audit.py
autonomous: true
requirements:
  - DETECT-02
  - DETECT-03

must_haves:
  truths:
    - "detect_anomalies() flags SKUs where abs(deviation_pct) > threshold_pct and returns a DataFrame with deviation_pct and direction columns"
    - "SKUs with NaN baseline (first-ever snapshot) are excluded from anomaly output — no false positives on new SKUs"
    - "load_prior_counts() returns {(key, direction): consecutive_count} only for records dated exactly one prior run (±1 day tolerance per D-12 open question resolution)"
    - "classify_sustained_shifts() adds sustained_shift=True for rows where consecutive_weeks_flagged >= SUSTAINED_SHIFT_N"
    - "append_to_history() writes to anomaly_history.csv in append mode; writes header only when file does not exist"
    - "AnomalyRow Pydantic model validates each row before write; validate_anomaly_df() halts on ValidationError"
    - "All 6 remaining DETECT-02 and DETECT-03 tests turn GREEN"
  artifacts:
    - path: "run_audit.py"
      provides: "Detection layer: AnomalyRow, detect_anomalies, load_prior_counts, classify_sustained_shifts, append_to_history, validate_anomaly_df"
      contains: "AnomalyRow"
  key_links:
    - from: "run_audit.py classify_sustained_shifts()"
      to: "anomaly_history.csv"
      via: "load_prior_counts() dict lookup"
      pattern: "HISTORY_PATH"
    - from: "run_audit.py append_to_history()"
      to: "anomaly_history.csv"
      via: "to_csv(mode='a', header=not HISTORY_PATH.exists())"
      pattern: "mode=\"a\""
---

<objective>
Wave 2: Detection layer. Replaces the detect_anomalies() stub from Wave 1 with the full implementation: threshold flagging, consecutive count lookup with continuity check, sustained-shift classification, and anomaly_history.csv append. All 6 remaining DETECT-02 and DETECT-03 tests turn GREEN.

Purpose: After this wave, the audit system can detect anomalies and track them across runs. Sustained-shift suppression (D-11, D-12, D-13) prevents alert fatigue on permanent fee changes.
Output: AnomalyRow Pydantic model, detect_anomalies(), load_prior_counts(), classify_sustained_shifts(), append_to_history(), validate_anomaly_df() — all in run_audit.py.
</objective>

<execution_context>
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\.planning\phases\02-detection-output-pipeline\02-RESEARCH.md
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\.planning\phases\02-detection-output-pipeline\02-PATTERNS.md
</execution_context>

<context>
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\.planning\phases\02-detection-output-pipeline\02-CONTEXT.md
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\explore_fees.py
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\.planning\phases\02-detection-output-pipeline\02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AnomalyRow model and detect_anomalies() replacing the stub</name>
  <files>run_audit.py</files>
  <behavior>
    - AnomalyRow(BaseModel) has exactly the D-12 HISTORY_COLUMNS fields: key_sales_marketplace_sku (str), country (str), sku (Optional[str]), asin (Optional[str]), sales_region (Optional[str]), week_start_date (datetime.date), avg_fee_per_unit (float), baseline_median_fee_per_unit (float), deviation_pct (float), direction (str), consecutive_weeks_flagged (int), run_date (datetime.date)
    - detect_anomalies(current_df, combined_history_df, threshold_pct) returns only rows where abs(deviation_pct) > threshold_pct (not >=)
    - detect_anomalies() drops rows with NaN baseline before flagging — no KeyError, no false positives on first-seen SKUs
    - detect_anomalies() sets direction = "increase" if deviation_pct > 0 else "decrease"
    - detect_anomalies() derives country from the key prefix (first token before " | ") — same logic as explore_fees.extract_country()
    - test_rolling_baseline_excludes_current_week: remains GREEN (baseline computation unchanged)
    - test_anomaly_threshold_15pct: turns GREEN with full detect_anomalies() replacing stub
    - validate_anomaly_df(df) follows validate_output_df() pattern from explore_fees.py: instantiates AnomalyRow with first row's dict (replacing NaN with None for Optional fields); prints "Schema validation: OK (N rows)" or raises on ValidationError
  </behavior>
  <action>
    Replace the detect_anomalies() stub (which raised NotImplementedError) with the full implementation. Add AnomalyRow and validate_anomaly_df() in the same edit.

    AnomalyRow — place immediately after the load_config() function (before data layer functions). Follow FeeRow pattern from explore_fees.py (lines 79-103): Pydantic BaseModel with Optional for sku, asin, sales_region (these are NaN for amzn.gr.* keys). Docstring: "Pydantic v2 model for one anomaly row written to anomaly_history.csv and the ClickUp CSV attachment. Matches HISTORY_COLUMNS per D-12."

    detect_anomalies(current_df: pd.DataFrame, combined_history_df: pd.DataFrame, threshold_pct: float) -> pd.DataFrame:
    Implementation mirrors the complete function in RESEARCH.md Code Examples section:
    1. Sort combined_history_df by ["key_sales_marketplace_sku", "week_start_date"]
    2. Compute baseline_median_fee_per_unit via groupby transform with shift(1).rolling(window=8, min_periods=1).median()
    3. current_week_date = current_df["week_start_date"].max()
    4. Slice combined rows where week_start_date == current_week_date
    5. dropna(subset=["baseline_median_fee_per_unit"])
    6. Compute deviation_pct = (avg_fee_per_unit - baseline) / baseline * 100
    7. Filter where abs(deviation_pct) > threshold_pct
    8. Add direction column
    9. Add country column: key.split(" | ")[0] — handle amzn.gr.* keys (no " | ") by using key itself as country placeholder
    10. Return flagged DataFrame

    validate_anomaly_df(df: pd.DataFrame) -> None:
    Same pattern as explore_fees.validate_output_df() (PATTERNS.md validation pattern section). If df.empty: print warning and return. first_row = df.iloc[0].where(df.iloc[0].notna(), other=None).to_dict(). Coerce week_start_date and run_date to .date() if they are Timestamps. AnomalyRow(**first_row). Print "Schema validation: OK (N rows)".

    After adding these functions, run test_anomaly_threshold_15pct to confirm it turns GREEN.
  </action>
  <verify>
    <automated>pytest tests/test_detection.py::test_anomaly_threshold_15pct tests/test_detection.py::test_rolling_baseline_excludes_current_week tests/test_detection.py::test_sparse_baseline_min_periods -x -q</automated>
  </verify>
  <done>All 3 DETECT-02 tests pass. AnomalyRow is importable. validate_anomaly_df is importable. detect_anomalies() returns only rows where abs(deviation_pct) > threshold_pct (strict inequality).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add load_prior_counts(), classify_sustained_shifts(), append_to_history()</name>
  <files>run_audit.py</files>
  <behavior>
    - load_prior_counts(current_run_date: datetime.date) -> dict: returns empty dict if HISTORY_PATH does not exist; otherwise reads anomaly_history.csv, groups by (key_sales_marketplace_sku, direction), takes last record per group, filters to records where abs((last_run_date - (current_run_date - 7 days)).days) <= 1 (±1 day tolerance per D-12 open question resolution); returns {(key, direction): consecutive_weeks_flagged}
    - test_consecutive_count_increments: GREEN — history has run_date = today - 7 days, count=2; load_prior_counts returns {(key, "increase"): 2}; after classify_sustained_shifts the row has consecutive_weeks_flagged=3
    - test_consecutive_count_resets_on_gap: GREEN — history has run_date = today - 14 days; load_prior_counts returns {} for that key (outside ±1 day window); classify_sustained_shifts sets consecutive_weeks_flagged=1
    - classify_sustained_shifts(anomalies_df, prior_counts, sustained_shift_n) -> pd.DataFrame: adds consecutive_weeks_flagged column (prior_count + 1, or 1 if not in prior_counts); adds sustained_shift column (consecutive_weeks_flagged >= sustained_shift_n)
    - test_sustained_shift_classification: GREEN — SKU with consecutive_weeks_flagged=4 gets sustained_shift=True; SKU with 3 gets False
    - append_to_history(new_rows: pd.DataFrame) -> None: writes with mode="a", header=not HISTORY_PATH.exists(); columns ordered per HISTORY_COLUMNS constant; calls validate_anomaly_df(new_rows) before writing
  </behavior>
  <action>
    Add three functions to run_audit.py in the detection layer section (after detect_anomalies() and validate_anomaly_df(), before the main() stub).

    load_prior_counts(current_run_date: datetime.date) -> dict:
    Implement verbatim from RESEARCH.md Pattern 3 with ±1 day tolerance modification:
    - If not HISTORY_PATH.exists(): return {}
    - history = pd.read_csv(HISTORY_PATH, parse_dates=["run_date"])
    - last_records = history.sort_values("run_date").groupby(["key_sales_marketplace_sku", "direction"]).last().reset_index()
    - prev_expected = pd.Timestamp(current_run_date) - pd.Timedelta(weeks=1)
    - CRITICAL: use abs((last_records["run_date"] - prev_expected).dt.days) <= 1 instead of strict equality (±1 day tolerance for delayed runs)
    - prior = last_records[abs_delta_series]
    - Return dict(zip(zip(prior["key_sales_marketplace_sku"], prior["direction"]), prior["consecutive_weeks_flagged"]))

    classify_sustained_shifts(anomalies_df: pd.DataFrame, prior_counts: dict, sustained_shift_n: int) -> pd.DataFrame:
    - result = anomalies_df.copy()
    - consecutive_func = lambda row: prior_counts.get((row["key_sales_marketplace_sku"], row["direction"]), 0) + 1
    - result["consecutive_weeks_flagged"] = result.apply(consecutive_func, axis=1)
    - result["sustained_shift"] = result["consecutive_weeks_flagged"] >= sustained_shift_n
    - Add run_date column: datetime.date.today() (this is needed for append_to_history)
    - Return result

    append_to_history(new_rows: pd.DataFrame) -> None:
    - validate_anomaly_df(new_rows) — halt before write if schema invalid
    - Select and reorder columns to match HISTORY_COLUMNS exactly: new_rows[HISTORY_COLUMNS]
    - write_header = not HISTORY_PATH.exists()
    - new_rows[HISTORY_COLUMNS].to_csv(HISTORY_PATH, mode="a", header=write_header, index=False)
    - print(f"Appended {len(new_rows)} rows to {HISTORY_PATH}")

    After adding these three functions, run the full DETECT-03 test suite to confirm all 3 tests are GREEN. Then run the full test suite to confirm no regression on Phase 1 tests.
  </action>
  <verify>
    <automated>pytest tests/test_detection.py::test_consecutive_count_increments tests/test_detection.py::test_consecutive_count_resets_on_gap tests/test_detection.py::test_sustained_shift_classification -x -q</automated>
  </verify>
  <done>All 3 DETECT-03 tests pass. Full test suite check: pytest tests/ -x -q shows 9 Phase 1 tests + 6 DETECT-02/DETECT-03 tests = 15 passing. Remaining 5 tests (OUT-01, OUT-02, OUT-03, ESC-01) still fail — expected, as they require Wave 3 functions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anomaly_history.csv → load_prior_counts() | Local file; written by this same script. Contents are validated via AnomalyRow before being written. Trust level: internal. |
| anomaly_history.csv ← append_to_history() | validate_anomaly_df() gate before any write — prevents malformed rows from corrupting state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | anomaly_history.csv consecutive count lookup | mitigate | Continuity check (±1 day tolerance) prevents count inflation after gap weeks. Documented in RESEARCH.md Pitfall 2. Enforced by test_consecutive_count_resets_on_gap. |
| T-02-07 | Tampering | AnomalyRow schema before CSV write | mitigate | validate_anomaly_df() calls AnomalyRow(**first_row) — raises ValidationError before to_csv() if schema is wrong. Mirrors T-04-02 pattern from Phase 1. |
| T-02-08 | Information Disclosure | SKU names in anomaly JSON (prompt injection risk) | accept | SKU names come from PBI (internal data, not user input). Risk is LOW for this internal tool. D-19 already gates: Claude receives JSON summary, not raw rows. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. |
</threat_model>

<verification>
After both tasks complete:
- pytest tests/test_detection.py -k "DETECT" -q → 6 passed (all DETECT-02 and DETECT-03 tests)
- pytest tests/ -x -q → 15 passed (9 Phase 1 + 6 new), 5 remaining fail with ImportError/NotImplementedError on OUT-01/02/03/ESC-01 functions
- python -c "from run_audit import AnomalyRow, detect_anomalies, load_prior_counts, classify_sustained_shifts, append_to_history, validate_anomaly_df" exits 0
</verification>

<success_criteria>
Wave 2 is complete when: all 6 DETECT-02 and DETECT-03 tests are GREEN. AnomalyRow, detect_anomalies(), load_prior_counts(), classify_sustained_shifts(), append_to_history(), and validate_anomaly_df() are all importable and tested. The ±1 day continuity tolerance is implemented (verified by test_consecutive_count_resets_on_gap). All 9 Phase 1 tests remain green.
</success_criteria>

<output>
Create `.planning/phases/02-detection-output-pipeline/02-03-SUMMARY.md` when done.
</output>
