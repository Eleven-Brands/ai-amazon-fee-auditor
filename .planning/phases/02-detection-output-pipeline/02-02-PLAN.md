---
phase: 02-detection-output-pipeline
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - run_audit.py
autonomous: true
requirements:
  - DETECT-02

must_haves:
  truths:
    - "On first run (snapshots/ empty): historical_batch_fetch() executes 10 DAX calls and saves one snapshot CSV per distinct week_start_date found in the response"
    - "On subsequent runs (snapshots/ non-empty): snapshot_fetch() executes 1 DAX call (is_latest=1) and saves snapshot_YYYYMMDD.csv"
    - "load_snapshots(n=8) returns a combined DataFrame sorted by (key_sales_marketplace_sku, week_start_date)"
    - "compute_baseline() applies shift(1).rolling(window=8, min_periods=1).median() per SKU group — current week is excluded from its own baseline"
    - "If fewer than 4 snapshot files exist, a WARNING is printed to stdout (D-06)"
    - "3 DETECT-02 baseline unit tests turn GREEN (test_rolling_baseline_excludes_current_week, test_anomaly_threshold_15pct, test_sparse_baseline_min_periods)"
  artifacts:
    - path: "run_audit.py"
      provides: "Data layer functions: load_config, build_snapshot_dax, build_historical_dax, build_amzn_gr_dax, save_snapshot, load_snapshots, compute_baseline, historical_batch_fetch, snapshot_fetch"
      min_lines: 120
  key_links:
    - from: "run_audit.py"
      to: "explore_fees.py"
      via: "from explore_fees import get_token, run_dax, process_pbi_rows, build_output_df, WORKSPACE_ID, DATASET_ID, SCOPES, CACHE_PATH, COUNTRY_CURRENCY"
      pattern: "from explore_fees import"
    - from: "run_audit.py save_snapshot()"
      to: "snapshots/snapshot_YYYYMMDD.csv"
      via: "pathlib.Path / to_csv()"
      pattern: "SNAPSHOTS_DIR"
---

<objective>
Wave 1: Data layer. Creates run_audit.py with all functions responsible for fetching Power BI data and computing the rolling 8-week median baseline. No detection logic, no ClickUp calls — pure data in, baseline out.

Purpose: Turns 3 of 11 unit tests GREEN (the DETECT-02 baseline tests). Establishes the snapshot accumulation strategy (D-01, D-04) and the rolling median computation (D-05).
Output: run_audit.py with load_config(), build_snapshot_dax(), build_historical_dax(prefix), build_amzn_gr_dax(), save_snapshot(), load_snapshots(), compute_baseline(), historical_batch_fetch(), snapshot_fetch().
</objective>

<execution_context>
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\.planning\phases\02-detection-output-pipeline\02-RESEARCH.md
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\.planning\phases\02-detection-output-pipeline\02-PATTERNS.md
</execution_context>

<context>
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\.planning\phases\02-detection-output-pipeline\02-CONTEXT.md
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\explore_fees.py
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\.planning\phases\02-detection-output-pipeline\02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create run_audit.py — module skeleton, imports, constants, load_config()</name>
  <files>run_audit.py</files>
  <behavior>
    - load_config() with no arguments reads audit_config.json from project root, returns a dict with keys THRESHOLD_PCT (float/int), CLICKUP_TASK_ID (str), RECIPIENTS (list), SUSTAINED_SHIFT_N (int)
    - load_config() raises SystemExit with a clear message if CLICKUP_TASK_ID == "PLACEHOLDER" (per D-14 security guard in PATTERNS.md)
    - load_config() raises AssertionError (or SystemExit) if type assertions fail for any of the 4 keys (ASVS V5 input validation per PATTERNS.md audit_config.json section)
    - Module-level constants HISTORY_PATH, SNAPSHOTS_DIR, HISTORY_COLUMNS, COUNTRY_PREFIXES are defined before any function
    - All imports from explore_fees.py are present: get_token, run_dax, process_pbi_rows, build_output_df, iso_to_week_start, WORKSPACE_ID, DATASET_ID, SCOPES, CACHE_PATH, COUNTRY_CURRENCY
  </behavior>
  <action>
    Create run_audit.py. This file begins Phase 2. Do NOT copy any function implementations from explore_fees.py — only import them.

    File structure (follow PATTERNS.md run_audit.py section exactly):

    1. Module docstring: "FBA Fee Auditor — Phase 2 detection + output pipeline. Imports reusable functions from explore_fees.py (Phase 1). Entry point: python run_audit.py"

    2. Imports (standard library first, then third-party, then project):
       - Standard: datetime, io, json, os, pathlib
       - Third-party: anthropic, pandas as pd, requests, dotenv.load_dotenv, pydantic.BaseModel, pydantic.ValidationError, typing.Optional
       - Project: from explore_fees import get_token, run_dax, process_pbi_rows, build_output_df, iso_to_week_start, WORKSPACE_ID, DATASET_ID, SCOPES, CACHE_PATH, COUNTRY_CURRENCY

    3. Module-level constants (after imports, before any function):
       HISTORY_PATH = pathlib.Path("anomaly_history.csv")
       SNAPSHOTS_DIR = pathlib.Path("snapshots")
       HISTORY_COLUMNS = ["key_sales_marketplace_sku", "country", "sku", "asin", "sales_region", "week_start_date", "avg_fee_per_unit", "baseline_median_fee_per_unit", "deviation_pct", "direction", "consecutive_weeks_flagged", "run_date"]
       COUNTRY_PREFIXES = {"US": "US | ", "CA": "CA | ", "GB": "GB | ", "DE": "DE | ", "FR": "FR | ", "ES": "ES | ", "IT": "IT | ", "MX": "MX | ", "BE": "BE | "}

    4. load_config(config_path: pathlib.Path = pathlib.Path("audit_config.json")) -> dict:
       - Opens config_path, calls json.load()
       - Type assertions for each key (ASVS V5): assert isinstance(config["THRESHOLD_PCT"], (int, float)), assert isinstance(config["SUSTAINED_SHIFT_N"], int), assert isinstance(config["CLICKUP_TASK_ID"], str), assert isinstance(config["RECIPIENTS"], list)
       - Security guard: if config["CLICKUP_TASK_ID"] == "PLACEHOLDER": raise SystemExit("CLICKUP_TASK_ID is not configured. Edit audit_config.json and replace 'PLACEHOLDER' with a real ClickUp task ID.")
       - Returns the validated config dict

    At the end of the file, add the main() stub (empty body with just a pass — it will be completed in Wave 3):
       def main() -> None:
           pass
       if __name__ == "__main__":
           main()

    This stub allows the import in test_detection.py to resolve for functions added progressively across waves.
  </action>
  <verify>
    <automated>pytest tests/test_detection.py -x -q 2>&1 | head -5</automated>
  </verify>
  <done>pytest tests/test_detection.py no longer fails with "ModuleNotFoundError: No module named 'run_audit'". Tests may fail with ImportError on specific function names not yet defined — that is acceptable and expected. pytest tests/test_aggregation.py -x -q shows 9 passed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add data layer functions — DAX builders, snapshot I/O, baseline computation</name>
  <files>run_audit.py</files>
  <behavior>
    - build_snapshot_dax() returns a DAX EVALUATE string with is_latest=1 FILTER (same structure as build_fee_dax() in explore_fees.py)
    - build_historical_dax(prefix: str) returns a DAX EVALUATE string with FILTER(SUMMARIZECOLUMNS(...), LEFT(key, len(prefix)) = prefix) — no is_latest filter, no date range filter (per RESEARCH.md Pitfall 6 and Pattern 4)
    - build_amzn_gr_dax() returns a DAX EVALUATE string with LEFT(key, 7) = "amzn.gr" filter
    - save_snapshot(df, run_date) writes df to SNAPSHOTS_DIR/snapshot_YYYYMMDD.csv and returns the path
    - load_snapshots(n=8) reads the n most recent snapshot_*.csv files (sorted lexicographically = date order), concatenates them, returns a combined DataFrame with week_start_date as pd.Timestamp; prints WARNING if len(files) < 4 (D-06)
    - compute_baseline(combined_df) adds baseline_median_fee_per_unit column using groupby key_sales_marketplace_sku transform: lambda x: x.shift(1).rolling(window=8, min_periods=1).median() — this is the EXACT shift(1) pattern from RESEARCH.md Pattern 1
    - historical_batch_fetch(token) runs 10 DAX calls (COUNTRY_PREFIXES 9 entries + build_amzn_gr_dax()), concatenates results via process_pbi_rows(), calls build_output_df(), splits into per-week DataFrames and saves each as a snapshot; returns combined DataFrame
    - snapshot_fetch(token) runs 1 DAX call using build_snapshot_dax(), calls process_pbi_rows(), calls build_output_df(), calls save_snapshot(); returns the snapshot DataFrame
    - test_rolling_baseline_excludes_current_week: MUST turn GREEN after this task
    - test_anomaly_threshold_15pct: MUST turn GREEN after this task (depends on detect_anomalies() being importable — add a stub if needed)
    - test_sparse_baseline_min_periods: MUST turn GREEN after this task
  </behavior>
  <action>
    Add data layer functions to run_audit.py immediately above the main() stub. Functions must be added in dependency order (DAX builders first, then save/load, then compute, then batch/snapshot fetch).

    build_snapshot_dax() -> str:
    Returns the same DAX as explore_fees.build_fee_dax() — SUMMARIZECOLUMNS on key_sales_marketplace_sku and date_fee_preview with is_latest=1 FILTER. Column alias: "avg_fee_per_unit". No ORDER BY needed.

    build_historical_dax(prefix: str) -> str:
    Returns DAX from RESEARCH.md Pattern 4. FILTER wraps SUMMARIZECOLUMNS. No is_latest filter. No date range filter. LEFT() check on key_sales_marketplace_sku. prefix_len = len(prefix). Column alias: "avg_fee_per_unit".

    build_amzn_gr_dax() -> str:
    Returns DAX from RESEARCH.md Pattern 4 amzn.gr section. LEFT(key, 7) = "amzn.gr". No is_latest filter.

    save_snapshot(df: pd.DataFrame, run_date: datetime.date) -> pathlib.Path:
    SNAPSHOTS_DIR.mkdir(exist_ok=True). path = SNAPSHOTS_DIR / f"snapshot_{run_date.strftime('%Y%m%d')}.csv". df.to_csv(path, index=False). print(f"Snapshot saved: {path}"). return path.

    load_snapshots(n: int = 8) -> pd.DataFrame:
    snapshot_files = sorted(SNAPSHOTS_DIR.glob("snapshot_*.csv")). If len < 4, print WARNING (D-06 exact text: "WARNING: baseline computed from {N} weeks — results may be noisy until 4+ weeks accumulate."). window_files = snapshot_files[-n:]. Load each with pd.read_csv(f, parse_dates=["week_start_date"]). pd.concat all, ignore_index=True. Return combined DataFrame.

    compute_baseline(combined_df: pd.DataFrame) -> pd.DataFrame:
    Sort by ["key_sales_marketplace_sku", "week_start_date"]. Apply groupby transform with shift(1).rolling(window=8, min_periods=1).median() on avg_fee_per_unit. Assign result to baseline_median_fee_per_unit column. Return df.

    historical_batch_fetch(token: str) -> pd.DataFrame:
    Iterate over COUNTRY_PREFIXES.values() (9 prefixes) + ["amzn.gr"]. For each: call appropriate DAX builder, call run_dax(dax, token), call process_pbi_rows(rows). Concatenate all results into fee_df. Call build_output_df(fee_df, sku_df) where sku_df is fetched once (run_dax with SKU_QUERY from explore_fees — import SKU_QUERY constant or define the query inline). Group combined_df by week_start_date (derived from date_fee_preview). For each distinct week: save_snapshot(week_df, week_date). Print progress per batch: "Fetching historical batch: {prefix} ({i}/{total})". Return combined DataFrame.

    Note on SKU_QUERY: it is defined as a module-level constant in explore_fees.py. Add it to the import line: from explore_fees import ..., SKU_QUERY. If SKU_QUERY is not importable (not exported), define the identical DAX inline in run_audit.py as HISTORICAL_SKU_QUERY.

    snapshot_fetch(token: str) -> pd.DataFrame:
    today = datetime.date.today(). run_dax(build_snapshot_dax(), token) -> rows. process_pbi_rows(rows) -> fee_df. Fetch sku_df (same SKU DAX). build_output_df(fee_df, sku_df) -> df. Add week_start_date column if not already present (derive from date_fee_preview using iso_to_week_start). save_snapshot(df, today). Return df.

    Also add a stub for detect_anomalies() so test_anomaly_threshold_15pct can import it (even if it raises NotImplementedError — the test mocks the call):
    def detect_anomalies(current_df, combined_history_df, threshold_pct): raise NotImplementedError

    After writing all functions, run the 3 DETECT-02 baseline tests. If test_rolling_baseline_excludes_current_week fails, debug the shift(1) pattern against RESEARCH.md Pattern 1. The test constructs a DataFrame directly and calls compute_baseline() — no mocking needed for this test.
  </action>
  <verify>
    <automated>pytest tests/test_detection.py::test_rolling_baseline_excludes_current_week tests/test_detection.py::test_anomaly_threshold_15pct tests/test_detection.py::test_sparse_baseline_min_periods -x -q</automated>
  </verify>
  <done>All 3 DETECT-02 baseline tests pass. pytest tests/ -x -q shows: 9 Phase 1 tests pass, 3 DETECT-02 tests pass, remaining 8 tests fail with ImportError or NotImplementedError (expected — subsequent waves implement those functions).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Power BI API response → run_audit.py | Semi-trusted: rows are from OrganiHaus's own PBI dataset; process_pbi_rows() strips bracket formatting before use |
| audit_config.json → load_config() | Local file; type-asserted on load; CLICKUP_TASK_ID placeholder check enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | CLICKUP_API_KEY in audit_config.json | mitigate | load_config() only reads the 4 expected keys; key assertions ensure no accidental secret leaks via wrong file. CLICKUP_API_KEY stays in .env only. |
| T-02-04 | Tampering | Historical DAX date filter | accept | Do NOT add date range filter to historical DAX — documented in RESEARCH.md Pitfall 6. No user input reaches the DAX string (prefix values come from COUNTRY_PREFIXES constant). |
| T-02-05 | Denial of Service | PBI value limit (1M rows × cols) | mitigate | Country-batched queries: worst case ~54K values per batch (well under 1M). Documented in RESEARCH.md Pattern 4. No validate_value_count() guard needed per research finding. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs in this plan — anthropic==0.101.0 already added in Wave 0. |
</threat_model>

<verification>
After both tasks complete:
- pytest tests/test_detection.py::test_rolling_baseline_excludes_current_week -x -q → PASSED
- pytest tests/test_detection.py::test_anomaly_threshold_15pct -x -q → PASSED
- pytest tests/test_detection.py::test_sparse_baseline_min_periods -x -q → PASSED
- pytest tests/test_aggregation.py -x -q → 9 passed (regression clean)
- python -c "from run_audit import load_config, build_snapshot_dax, build_historical_dax, build_amzn_gr_dax, save_snapshot, load_snapshots, compute_baseline" exits with code 0
</verification>

<success_criteria>
Wave 1 is complete when: 3 DETECT-02 tests green. run_audit.py importable. All 9 Phase 1 tests still green. The rolling baseline uses shift(1) (verified by test_rolling_baseline_excludes_current_week passing). Historical DAX has no date filter (per D-01 and RESEARCH.md Pitfall 6).
</success_criteria>

<output>
Create `.planning/phases/02-detection-output-pipeline/02-02-SUMMARY.md` when done.
</output>
