---
phase: 02-detection-output-pipeline
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - run_audit.py
autonomous: true
requirements:
  - DETECT-02
  - DETECT-03

must_haves:
  truths:
    - "filter_valid_skus() returns ~484 keys after 3-part filter (Active status, valid lifecycle, inventory max_date >= today-90)"
    - "Fee+velocity+11B query returns fee_current, fee_11b, velocity_w3, native_family, sales_region, country per valid key"
    - "Snapshot saved to snapshots/snapshot_YYYYMMDD.csv each run"
    - "Query A and Query B are separate PBI calls joined in Python (not a single all-in-one DAX)"
    - "Inventory Ledger query uses Key Column: Country | SKU (not EU-region aggregate measure)"
  artifacts:
    - path: "run_audit.py"
      provides: "filter_valid_skus(), build_sku_query_dax(), build_inv_ledger_dax(), build_fee_velocity_dax(), load_config(), save_snapshot(), fetch_fee_data()"
      exports: ["filter_valid_skus", "build_fee_velocity_dax", "load_config", "save_snapshot", "fetch_fee_data"]
  key_links:
    - from: "run_audit.py"
      to: "explore_fees.py"
      via: "from explore_fees import get_token, run_dax, process_pbi_rows"
      pattern: "from explore_fees import"
    - from: "run_audit.py"
      to: "Power BI REST API"
      via: "run_dax() called with DAX strings from build_* functions"
      pattern: "run_dax\\("
---

<objective>
Implement the data acquisition layer of run_audit.py: the triple-filter valid SKU list (D-24) and the combined fee+velocity+11B expected query (D-25, D-26).

This is the foundation every downstream plan depends on. Per D-24a the valid SKU filter runs two separate PBI queries (Query A: SKU status/lifecycle, Query B: Inventory Ledger qty+recency) and joins them in Python. Per D-25/D-26b the fee query retrieves fee_preview_current, fee_11b_expected, and velocity_w3 in a single SUMMARIZECOLUMNS call. Per D-04 a snapshot is saved each run.

Purpose: Produce a clean ~484-key valid_keys set and a fee_df DataFrame that downstream scoring can operate on without touching PBI again.
Output: run_audit.py containing load_config(), filter_valid_skus(), build_fee_velocity_dax(), fetch_fee_data(), save_snapshot(), and module constants.
</objective>

<execution_context>
@G:/Users/User VivoBook/.claude/get-shit-done/workflows/execute-plan.md
@G:/Users/User VivoBook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-detection-output-pipeline/02-CONTEXT.md
@.planning/phases/02-detection-output-pipeline/02-RESEARCH.md
@.planning/phases/02-detection-output-pipeline/02-PATTERNS.md
@explore_fees.py
@.planning/phases/02-detection-output-pipeline/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create run_audit.py with config loader, constants, and valid SKU filter</name>
  <files>run_audit.py</files>
  <behavior>
    - load_config() reads audit_config.json from project root; asserts CLICKUP_TASK_ID != "PLACEHOLDER" (raises SystemExit if placeholder); returns dict with CLICKUP_TASK_ID, RECIPIENTS, BACKLOG_WEIGHT, MONTHLY_CASE_LIMIT, FNSKU_CAP_DAYS
    - build_sku_query_dax() returns DAX string for Query A: SUMMARIZECOLUMNS on 'SKUs'[Key Column: Country | SKU], 'SKUs'[Life Cycle], 'SKUs'[status] with no FILTER wrapper (Python does the status/lifecycle filtering)
    - build_inv_ledger_dax() returns DAX string for Query B: SUMMARIZECOLUMNS on 'Inventory Ledger'[Key Column: Country | SKU] with measures max_date=MAX('Inventory Ledger'[Date]) and qty_sellable=CALCULATE(SUM('Inventory Ledger'[Qty Sellable]), 'Inventory Ledger'[Disposition]="Sellable") — per D-24 this is at Country|SKU level, NOT via any EU-region aggregate measure
    - filter_valid_skus(token) calls run_dax() twice, joins on key, filters to status=="Active" AND lifecycle not in [C,D] AND qty_sellable > 0 AND max_date >= today-90d; returns filtered DataFrame
    - save_snapshot(df, run_date) saves to snapshots/snapshot_YYYYMMDD.csv per D-04
  </behavior>
  <action>
    Create run_audit.py at project root. Follow explore_fees.py structure exactly (per PATTERNS.md):

    File header docstring: "FBA Fee Auditor — Phase 2 audit pipeline. Run: python run_audit.py"

    Imports (following PATTERNS.md import block):
      import datetime, io, json, os, pathlib
      from typing import Optional
      import anthropic
      import pandas as pd
      import requests
      from dotenv import load_dotenv
      from pydantic import BaseModel, ValidationError
      from explore_fees import (
          get_token, run_dax, process_pbi_rows,
          WORKSPACE_ID, DATASET_ID, SCOPES, CACHE_PATH,
      )

    Module constants (following PATTERNS.md constants block):
      HISTORY_PATH = pathlib.Path("anomaly_history.csv")
      SNAPSHOTS_DIR = pathlib.Path("snapshots")
      CASES_CSV_PATH = pathlib.Path("standalone_files/db_amazon_remeasurement_cases.csv")
      HISTORY_COLUMNS = ["key_sales_marketplace_sku", "country", "sku", "asin",
                         "sales_region", "week_start_date", "fee_preview_current",
                         "fee_11b_expected", "overcharge_per_unit", "velocity_w3",
                         "impact_4w", "priority_score", "consecutive_weeks_flagged",
                         "days_since_last_case", "eligible_for_case", "run_date"]
      SALES_REGIONS = ["US", "CA", "GB", "EU"]

    load_config(): reads audit_config.json with json.load(). Type-checks per PATTERNS.md type-check block:
      assert isinstance(config["BACKLOG_WEIGHT"], (int, float)), "BACKLOG_WEIGHT must be numeric"
      assert isinstance(config["MONTHLY_CASE_LIMIT"], int), "MONTHLY_CASE_LIMIT must be int"
      assert isinstance(config["FNSKU_CAP_DAYS"], int), "FNSKU_CAP_DAYS must be int"
      assert isinstance(config["CLICKUP_TASK_ID"], str), "CLICKUP_TASK_ID must be str"
      assert isinstance(config["RECIPIENTS"], list), "RECIPIENTS must be list"
      Then check CLICKUP_TASK_ID != "PLACEHOLDER" with SystemExit per PATTERNS.md error handling pattern.

    build_sku_query_dax(): returns triple-quoted string:
      EVALUATE
      SUMMARIZECOLUMNS(
          'SKUs'[Key Column: Country | SKU],
          'SKUs'[Life Cycle],
          'SKUs'[status]
      )

    build_inv_ledger_dax(): returns triple-quoted string:
      EVALUATE
      SUMMARIZECOLUMNS(
          'Inventory Ledger'[Key Column: Country | SKU],
          "max_date", MAX('Inventory Ledger'[Date]),
          "qty_sellable", CALCULATE(
              SUM('Inventory Ledger'[Qty Sellable]),
              'Inventory Ledger'[Disposition] = "Sellable"
          )
      )

    filter_valid_skus(token):
      1. rows_a = run_dax(build_sku_query_dax(), token) -> process_pbi_rows(rows_a) -> sku_df. Strip brackets from column names.
      2. rows_b = run_dax(build_inv_ledger_dax(), token) -> process_pbi_rows(rows_b) -> inv_df. Strip brackets.
      3. Normalize key column name in both DataFrames to 'key_country_sku' (PBI returns 'Key Column: Country | SKU' after bracket strip).
      4. Inner join: merged = sku_df.merge(inv_df, on='key_country_sku', how='inner')
      5. Filter conditions (all three required per D-24):
         a. merged['status'] == "Active"
         b. ~merged['Life Cycle'].str.upper().isin(["C", "D"])
         c. merged['qty_sellable'] > 0
         d. pd.to_datetime(merged['max_date']) >= pd.Timestamp(datetime.date.today()) - pd.Timedelta(days=90)
      6. Print "Valid SKUs after triple filter: {n}" per PATTERNS.md stdout pattern.
      7. Return merged[mask].copy()

    save_snapshot(df, run_date): SNAPSHOTS_DIR.mkdir(exist_ok=True). Select columns [key_sales_marketplace_sku, fee_preview_current] if present (snapshot is fee-per-key per D-04). Save to SNAPSHOTS_DIR / f"snapshot_{run_date.strftime('%Y%m%d')}.csv". Print "Snapshot saved: {path}".

    Do NOT implement main(), compute_overcharge, compute_priority_score, or any output functions yet.
  </action>
  <verify>
    <automated>cd "G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor" && python -c "from run_audit import filter_valid_skus, load_config, save_snapshot, build_sku_query_dax, build_inv_ledger_dax; print('imports OK')" && python -m pytest tests/test_detection.py -x -q -k "sku_validity" 2>&1</automated>
  </verify>
  <done>run_audit.py imports cleanly. test_sku_validity_filter_excludes_inactive and test_sku_validity_filter_excludes_stale_inventory both pass. load_config() raises SystemExit when CLICKUP_TASK_ID is "PLACEHOLDER".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fee + velocity + 11B expected query</name>
  <files>run_audit.py</files>
  <behavior>
    - build_fee_velocity_dax() returns a SUMMARIZECOLUMNS DAX string with four groupBy columns and three measures, no date filter (fee_11b uses ALL('Calendar') internally per D-25)
    - fetch_fee_data(valid_keys_df, token) calls run_dax, processes rows, filters to valid_keys set, casts numerics to float, returns DataFrame with normalized column names
    - Returned DataFrame columns: key_sales_marketplace_sku, native_family, sales_region, country, fee_preview_current, fee_11b_expected, velocity_w3
  </behavior>
  <action>
    Add the following two functions to run_audit.py (append after filter_valid_skus, do not modify existing functions):

    build_fee_velocity_dax(): returns this exact DAX string — single query, no country batching needed since valid_keys is ~484:
      EVALUATE
      SUMMARIZECOLUMNS(
          'SKUs'[Key Column: Country | SKU],
          'SKUs'[Native Family],
          'SKUs'[Sales Region],
          'SKUs'[Country],
          "fee_current", [$_unit_last_fba_fee_fee_preview],
          "fee_11b", [$_unit_last_fba_fee_calculation_expected],
          "velocity_w3", [m_sold_prev3]
      )

    Note per D-25: $_unit_last_fba_fee_calculation_expected uses ALL('Calendar') internally so no date filter is needed. Per D-26a: $_unit_last_fba_fee_fee_preview uses MAX('fact_fee_preview'[date_fee_preview]) context. Per D-27: m_sold_prev3 requires Calendar[YearWeekNum] context but evaluates correctly in SUMMARIZECOLUMNS per SKU.

    fetch_fee_data(valid_keys_df, token):
      1. rows = run_dax(build_fee_velocity_dax(), token)
      2. df = pd.DataFrame(process_pbi_rows(rows))
      3. Strip brackets from all column names: df.columns = [c.strip('[]') for c in df.columns]
      4. Rename columns to normalized names:
           'Key Column: Country | SKU' -> 'key_sales_marketplace_sku'
           'Native Family' -> 'native_family'
           'Sales Region' -> 'sales_region'
           'Country' -> 'country'
           'fee_current' -> 'fee_preview_current'
           'fee_11b' -> 'fee_11b_expected'
           (velocity_w3 retains its name from DAX alias)
      5. Filter to valid_keys_df: valid_set = set(valid_keys_df['key_country_sku']); df = df[df['key_sales_marketplace_sku'].isin(valid_set)].copy()
      6. Cast to float: for col in ['fee_preview_current', 'fee_11b_expected', 'velocity_w3']: df[col] = pd.to_numeric(df[col], errors='coerce')
      7. Print "Fee+velocity rows fetched: {len(df)} for {len(valid_keys_df)} valid keys"
      8. Return df

    Also update save_snapshot to accept the fee_df returned by fetch_fee_data and save [key_sales_marketplace_sku, fee_preview_current] columns plus a run_date column.
  </action>
  <verify>
    <automated>cd "G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor" && python -c "from run_audit import build_fee_velocity_dax, fetch_fee_data; dax = build_fee_velocity_dax(); checks = ['fee_current' in dax, 'fee_11b' in dax, 'velocity_w3' in dax, 'm_sold_prev3' in dax, 'Native Family' in dax]; assert all(checks), f'DAX missing expected content: {dax[:300]}'; print('DAX structure OK')" && python -m pytest tests/test_detection.py -x -q -k "sku_validity" 2>&1</automated>
  </verify>
  <done>build_fee_velocity_dax() returns DAX string with all four groupBy columns (Key Column: Country | SKU, Native Family, Sales Region, Country) and three measures (fee_current mapped to $_unit_last_fba_fee_fee_preview, fee_11b mapped to $_unit_last_fba_fee_calculation_expected, velocity_w3 mapped to m_sold_prev3). All sku_validity tests still pass. Phase 1 regression clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python -> Power BI REST API | PBI response data is trusted (internal OrganiHaus dataset); run_dax() raises on HTTP errors |
| PBI response -> Python DataFrame | Column names include brackets from PBI; process_pbi_rows() strips them — shape validated before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | CACHE_PATH token cache | accept | Token cache at ~/.claude/powerbi_token_cache.json (user home); same pattern as Phase 1; no change |
| T-02-04 | Tampering | audit_config.json type assertions | mitigate | load_config() asserts type of each field on load; malformed config raises AssertionError before any API call |
| T-02-05 | Denial of Service | PBI query timeout | mitigate | run_dax() has timeout=120s (inherited from explore_fees.py); no unbounded wait |
| T-02-SC | Tampering | anthropic==0.101.0 package install | mitigate | Package Legitimacy Audit passed (RESEARCH.md, slopcheck OK 2026-05-28); install via requirements.txt only |
</threat_model>

<verification>
After Wave 1 completes:
  python -m pytest tests/test_detection.py -x -q -k "sku_validity"
  Expected: 2 sku_validity tests pass

  python -m pytest tests/test_aggregation.py -x -q
  Expected: all Phase 1 tests still green

  python -c "from run_audit import filter_valid_skus, load_config, build_fee_velocity_dax, fetch_fee_data, save_snapshot; print('all imports OK')"
  Expected: no ImportError
</verification>

<success_criteria>
- run_audit.py exists and imports cleanly
- load_config() raises SystemExit when CLICKUP_TASK_ID == "PLACEHOLDER"
- build_fee_velocity_dax() returns DAX with $_unit_last_fba_fee_fee_preview, $_unit_last_fba_fee_calculation_expected, m_sold_prev3
- test_sku_validity_filter_excludes_inactive and test_sku_validity_filter_excludes_stale_inventory pass
- Phase 1 regression clean: pytest tests/test_aggregation.py -x -q green
</success_criteria>

<output>
Create `.planning/phases/02-detection-output-pipeline/02-02-SUMMARY.md` when done.
</output>
