---
phase: 02-detection-output-pipeline
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_detection.py
  - tests/conftest.py
  - snapshots/.gitkeep
  - .gitignore
  - audit_config.json
  - requirements.txt
  - .env.example
autonomous: true
requirements:
  - DETECT-02
  - DETECT-03
  - OUT-01
  - OUT-02
  - OUT-03
  - ESC-01

must_haves:
  truths:
    - "pytest tests/test_detection.py -x -q fails with ImportError (run_audit not yet implemented)"
    - "All 11 test names exist in test_detection.py and are recognized by pytest --collect-only"
    - "audit_config.json exists at project root with updated D-08 schema (no THRESHOLD_PCT/SUSTAINED_SHIFT_N, has BACKLOG_WEIGHT/MONTHLY_CASE_LIMIT/FNSKU_CAP_DAYS)"
    - "snapshots/.gitkeep exists and snapshots/* is gitignored"
    - "requirements.txt contains anthropic==0.101.0"
    - ".env.example contains CLICKUP_API_KEY= placeholder"
  artifacts:
    - path: "tests/test_detection.py"
      provides: "11 RED unit tests for Phase 2 detection + output logic"
    - path: "tests/conftest.py"
      provides: "Phase 2 fixtures appended (sample_snapshot_df, sample_audit_config, sample_anomaly_df)"
    - path: "audit_config.json"
      provides: "Runtime calibration config per D-08 updated schema"
    - path: "snapshots/.gitkeep"
      provides: "snapshots/ directory marker"
  key_links:
    - from: "tests/test_detection.py"
      to: "run_audit.py"
      via: "from run_audit import ..."
      pattern: "from run_audit import"
---

<objective>
Create the Wave 0 TDD scaffold for Phase 2. All test files are written in RED state (ImportError until Wave 1+ provides run_audit.py). All supporting files (config, directory markers, requirements) are created in their final state.

This plan implements D-24 through D-32 test contracts — the new overcharge-vs-11B-expected design. The OLD rolling-median-based test names (test_rolling_baseline_excludes_current_week, test_anomaly_threshold_15pct, etc.) from VALIDATION.md are superseded by the names below.

Purpose: Gate all future plans on passing tests. No plan past Wave 0 ships without a green pytest run.
Output: test_detection.py (11 RED tests), conftest.py additions, audit_config.json, snapshots/.gitkeep, .gitignore additions, requirements.txt addition, .env.example addition.
</objective>

<execution_context>
@G:/Users/User VivoBook/.claude/get-shit-done/workflows/execute-plan.md
@G:/Users/User VivoBook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-detection-output-pipeline/02-CONTEXT.md
@.planning/phases/02-detection-output-pipeline/02-PATTERNS.md
@.planning/phases/02-detection-output-pipeline/02-VALIDATION.md
@explore_fees.py
@tests/conftest.py
@tests/test_aggregation.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED test suite for new overcharge + prioritization design</name>
  <files>tests/test_detection.py</files>
  <behavior>
    All 11 tests must be written in RED state. Import `from run_audit import (filter_valid_skus, compute_overcharge, compute_priority_score, load_cases_csv, get_slots_remaining, get_sku_eligible, build_anomaly_json, generate_narrative, post_clickup_comment, attach_csv_to_task, load_config)`. run_audit.py does not exist yet — tests fail with ImportError on collection, which is the correct RED state.

    Test contracts (per user instructions):

    test_overcharge_positive_only — Given a DataFrame with fee_current=[3.0, 2.5] and fee_11b=[2.8, 3.0], compute_overcharge() returns only the row where fee_current > fee_11b (overcharge=0.20). The row with negative overcharge (Amazon charging less) is NOT in the returned DataFrame.

    test_sku_validity_filter_excludes_inactive — Given sku_df with status values ["Active", "Inactive", "Active"] and lifecycle values ["M", "M", "C"], filter_valid_skus() returns only the one row that is Active AND lifecycle not in (C, D). Two rows are excluded: one by status, one by lifecycle.

    test_sku_validity_filter_excludes_stale_inventory — Given inv_df with max_date values [today-30, today-91], filter_valid_skus() (or the inner join + date filter step) returns only the row where max_date >= today - 90. The row with max_date 91 days ago is excluded.

    test_priority_score_formula — Given overcharge=0.50, velocity_w3=100, consecutive_weeks_flagged=2, compute_priority_score() returns impact_4w=200.0 (0.50 * 100 * 4) and priority_score=280.0 (200.0 * (1 + 2 * 0.2)). Verify both values from the returned row/dict.

    test_backlog_weight_increases_rank — Given two SKUs: SKU-A with impact_4w=200 and consecutive_weeks_flagged=2 (priority_score=280), SKU-B with impact_4w=250 and consecutive_weeks_flagged=0 (priority_score=250). After sorting by priority_score descending, SKU-A ranks above SKU-B despite lower base impact.

    test_slots_remaining_by_region — Given a cases DataFrame with 3 rows for region="US" where Date Closed is within the current calendar month (>= month_start), get_slots_remaining(cases_df, "US", monthly_limit=10) returns 7 (10 - 3). A row outside the current month does not count.

    test_fnsku_cap_excludes_sku — Given a cases DataFrame with 2 rows for sku="US-OHFB-001" where Date Closed is within the last 60 days, get_sku_eligible(cases_df, "US-OHFB-001", cap_days=60, cap_count=2) returns False. A SKU with 1 closure in 60 days returns True.

    test_csv_attachment_columns — Given a minimal anomaly DataFrame, the CSV bytes produced by build_csv_bytes(df) contain all D-18 column headers: key_sales_marketplace_sku, country, sales_region, sku, asin, fee_preview_current, fee_11b_expected, overcharge_per_unit, velocity_w3, impact_4w, priority_score, consecutive_weeks_flagged, days_since_last_case, eligible_for_case, run_date.

    test_eu_output_has_country_column — Given an anomaly DataFrame with sales_region="EU" rows from DE/FR/ES, build_region_table(df, region="EU") returns a string containing the word "Country". build_region_table(df, region="US") does NOT contain "Country" column header.

    test_clickup_comment_has_four_sections — Given build_comment_body(regional_tables={"US": "...", "CA": "...", "GB": "...", "EU": "..."}, narratives={...}), the returned string contains all four region labels "US", "CA", "GB", "EU" as section headers.

    test_escalation_prompt_in_comment — The string returned by build_comment_body() ends with the exact literal: "Reply YES to this comment to open an investigation task for the top flagged SKUs."

    Section grouping: use comment banners per PATTERNS.md (DETECT-02, DETECT-03, OUT-01/ESC-01, OUT-02/OUT-03). Use unittest.mock for ClickUp and Anthropic calls. Use tmp_path + monkeypatch for any filesystem touches.
  </behavior>
  <action>
    Write tests/test_detection.py following the analog in tests/test_aggregation.py exactly: same file header docstring (reference Phase 2 requirements: DETECT-02, DETECT-03, OUT-01, OUT-02, OUT-03, ESC-01), same import block style, same section banner comments, same inline assertion message style ("function must behavior").

    Import block at top of file:
      import datetime, io, json, pathlib
      import pandas as pd
      import pytest
      from unittest.mock import patch, MagicMock
      from pydantic import ValidationError
      from run_audit import (
          filter_valid_skus, compute_overcharge, compute_priority_score,
          load_cases_csv, get_slots_remaining, get_sku_eligible,
          build_anomaly_json, generate_narrative,
          post_clickup_comment, attach_csv_to_task, load_config,
          build_csv_bytes, build_region_table, build_comment_body,
      )

    Note: run_audit.py does not exist — this import causes ImportError at collection, which is the correct RED state. Do NOT wrap in try/except. Do NOT use pytest.importorskip.

    Write all 11 tests exactly as specified in the behavior block. Each test is self-contained (constructs its own fixtures inline or uses tmp_path). No shared state between tests.
  </action>
  <verify>
    <automated>cd "G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor" && python -m pytest tests/test_detection.py --collect-only -q 2>&1 | head -5</automated>
  </verify>
  <done>pytest --collect-only on tests/test_detection.py shows ERROR with "ModuleNotFoundError: No module named 'run_audit'" — all 11 test names are listed in the collection error output. No SyntaxError.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold config files, directory markers, and fixtures</name>
  <files>
    tests/conftest.py
    audit_config.json
    snapshots/.gitkeep
    .gitignore
    requirements.txt
    .env.example
  </files>
  <action>
    1. audit_config.json — Create at project root with updated D-08 schema (THRESHOLD_PCT and SUSTAINED_SHIFT_N are removed per D-08 superseding; new keys per user spec):
       {"CLICKUP_TASK_ID": "PLACEHOLDER", "RECIPIENTS": [], "BACKLOG_WEIGHT": 0.2, "MONTHLY_CASE_LIMIT": 10, "FNSKU_CAP_DAYS": 60}

    2. snapshots/.gitkeep — Create empty file at snapshots/.gitkeep. Create the snapshots/ directory if it does not exist.

    3. .gitignore — Append two lines after the existing output/* block (pattern from PATTERNS.md):
       snapshots/*
       !snapshots/.gitkeep
       Do NOT remove any existing lines. Read .gitignore first, append only.

    4. requirements.txt — Append anthropic==0.101.0 as the last line. Read first to confirm it is not already present. Do NOT remove or reorder existing lines.

    5. .env.example — Read first. Add CLICKUP_API_KEY= as a new line if not already present. ANTHROPIC_API_KEY= may already be present from Phase 1; do not duplicate it.

    6. tests/conftest.py — Read the existing file first. Append three new fixtures at the bottom WITHOUT modifying any existing fixtures:
       - sample_snapshot_df: 8-row DataFrame with key="US | US-OHFB-001", week_start_date = date_range("2026-01-05", periods=8, freq="7D"), avg_fee_per_unit=[3.0,3.1,2.9,3.0,3.05,2.95,3.0,4.5]
       - sample_audit_config(tmp_path): writes {"CLICKUP_TASK_ID": "TEST_TASK_123", "RECIPIENTS": [], "BACKLOG_WEIGHT": 0.2, "MONTHLY_CASE_LIMIT": 10, "FNSKU_CAP_DAYS": 60} to tmp_path/audit_config.json; returns (cfg_path, config)
       - sample_anomaly_df: one-row DataFrame with all D-18 columns: key_sales_marketplace_sku="US | US-OHFB-001", country="US", sales_region="US", sku="US-OHFB-001", asin="B0EXAMPLE01", fee_preview_current=4.50, fee_11b_expected=3.00, overcharge_per_unit=1.50, velocity_w3=120, impact_4w=720.0, priority_score=720.0, consecutive_weeks_flagged=0, days_since_last_case=30, eligible_for_case=True, run_date=datetime.date(2026,5,28)

    Per PATTERNS.md: use pathlib.Path for all filesystem ops, follow the existing conftest.py docstring and fixture style exactly.
  </action>
  <verify>
    <automated>cd "G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor" && python -m pytest tests/ --collect-only -q 2>&1 | tail -5 && python -c "import json; c=json.load(open('audit_config.json')); assert c['BACKLOG_WEIGHT']==0.2 and c['MONTHLY_CASE_LIMIT']==10, f'Wrong config: {c}'; print('audit_config OK')" && python -c "import pathlib; assert pathlib.Path('snapshots/.gitkeep').exists(), 'gitkeep missing'; print('snapshots OK')" && python -c "reqs=open('requirements.txt').read(); assert 'anthropic==0.101.0' in reqs, 'anthropic missing'; print('requirements OK')"</automated>
  </verify>
  <done>audit_config.json has BACKLOG_WEIGHT=0.2, MONTHLY_CASE_LIMIT=10, FNSKU_CAP_DAYS=60 and no THRESHOLD_PCT key. snapshots/.gitkeep exists. requirements.txt contains anthropic==0.101.0. .gitignore contains "snapshots/*" and "!snapshots/.gitkeep". .env.example contains CLICKUP_API_KEY= line. conftest.py has three new fixtures without breaking existing Phase 1 tests (pytest tests/test_aggregation.py -x -q passes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test import | run_audit module does not exist yet; ImportError is expected and intentional |
| config write | audit_config.json written to project root; contains no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | .env.example | mitigate | CLICKUP_API_KEY= has blank value; actual key never committed; .env is gitignored |
| T-02-02 | Tampering | audit_config.json | accept | Contains only calibration values (no secrets); type assertions on load guard against malformed values |
| T-02-SC | Tampering | anthropic==0.101.0 install | mitigate | Package Legitimacy Audit completed in RESEARCH.md (slopcheck OK, verified 2026-05-28) |
</threat_model>

<verification>
Full Phase 1 regression must stay green after this plan:
  pytest tests/test_aggregation.py -x -q

Wave 0 red-state gate:
  pytest tests/test_detection.py --collect-only -q  -- must show 11 tests with ImportError, no SyntaxError
</verification>

<success_criteria>
- 11 tests collected in tests/test_detection.py, all fail with ImportError (run_audit not found)
- pytest tests/test_aggregation.py -x -q passes (Phase 1 regression clean)
- audit_config.json at project root with schema: CLICKUP_TASK_ID, RECIPIENTS, BACKLOG_WEIGHT, MONTHLY_CASE_LIMIT, FNSKU_CAP_DAYS
- snapshots/.gitkeep exists, snapshots/* in .gitignore
- requirements.txt has anthropic==0.101.0
- .env.example has CLICKUP_API_KEY= line
- conftest.py has three new Phase 2 fixtures, existing fixtures unmodified
</success_criteria>

<output>
Create `.planning/phases/02-detection-output-pipeline/02-01-SUMMARY.md` when done.
</output>
