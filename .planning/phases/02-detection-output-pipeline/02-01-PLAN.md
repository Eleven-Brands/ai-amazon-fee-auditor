---
phase: 02-detection-output-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_detection.py
  - tests/conftest.py
  - snapshots/.gitkeep
  - .gitignore
  - audit_config.json
  - requirements.txt
autonomous: true
requirements:
  - DETECT-02
  - DETECT-03
  - OUT-01
  - OUT-02
  - OUT-03
  - ESC-01

must_haves:
  truths:
    - "pytest tests/test_detection.py -x -q shows 11 ImportErrors (not collection errors)"
    - "snapshots/ directory exists and is tracked in git via .gitkeep"
    - "snapshots/*.csv entries are git-ignored; snapshots/.gitkeep is not"
    - "audit_config.json exists at project root with correct default values"
    - "requirements.txt contains anthropic==0.101.0"
    - "pytest tests/ -x -q still shows all Phase 1 tests green (no regression)"
  artifacts:
    - path: "tests/test_detection.py"
      provides: "11 unit tests for Phase 2 detection + output logic"
      min_lines: 150
    - path: "tests/conftest.py"
      provides: "Extended with 3 new Phase 2 fixtures"
      contains: "sample_snapshot_df"
    - path: "snapshots/.gitkeep"
      provides: "Empty sentinel file to track snapshots/ directory in git"
    - path: "audit_config.json"
      provides: "Runtime calibration config with 4 keys"
      contains: "THRESHOLD_PCT"
    - path: "requirements.txt"
      provides: "Dependency manifest"
      contains: "anthropic==0.101.0"
  key_links:
    - from: "tests/test_detection.py"
      to: "run_audit.py"
      via: "from run_audit import ..."
      pattern: "from run_audit import"
---

<objective>
Wave 0 infrastructure scaffold for Phase 2. Creates all prerequisite files that must exist before any production code is written. Establishes TDD discipline by writing 11 unit tests in RED state (ImportError on `from run_audit import ...`). Sets up directory structure, default config, and dependency pin.

Purpose: No Wave 1 implementation task can start until these scaffolds exist. The test file is the contract that subsequent plans implement against.
Output: test_detection.py (11 RED tests), conftest.py additions, snapshots/.gitkeep, audit_config.json, .gitignore update, requirements.txt update.
</objective>

<execution_context>
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\.planning\phases\02-detection-output-pipeline\02-VALIDATION.md
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\.planning\phases\02-detection-output-pipeline\02-PATTERNS.md
</execution_context>

<context>
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\.planning\ROADMAP.md
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\.planning\phases\02-detection-output-pipeline\02-CONTEXT.md
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\tests\test_aggregation.py
@G:\Shared drives\OrganiHaus\3.1 - OH Data & Reports\z_personal_folders\lucca_lanzellotti\Projetos\AI Amazon Fee Auditor\tests\conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_detection.py with 11 RED unit tests</name>
  <files>tests/test_detection.py</files>
  <behavior>
    - test_rolling_baseline_excludes_current_week: given 8-week DataFrame for one SKU where week 8 fee spikes to 4.5 (prior weeks ~3.0), detect_anomalies() must flag week 8 with deviation_pct > 15
    - test_anomaly_threshold_15pct: a SKU with 14% deviation must NOT be flagged; a SKU with 16% deviation must be flagged
    - test_sparse_baseline_min_periods: a 3-week DataFrame (< 8 weeks) must not raise; baseline computed from available rows via min_periods=1
    - test_consecutive_count_increments: given history CSV with SKU flagged 2 prior weeks, load_prior_counts() returns count=2; after classify_sustained_shifts() the new row has consecutive_weeks_flagged=3
    - test_consecutive_count_resets_on_gap: history has last run_date 14 days ago (gap week); load_prior_counts() returns 0 for that SKU (continuity check resets count)
    - test_sustained_shift_classification: SKU with consecutive_weeks_flagged >= 4 gets sustained_shift=True; SKU with 3 gets sustained_shift=False
    - test_generate_narrative_payload: generate_narrative() called with mock Anthropic client; the mocked messages.create() receives a user message containing "fee_increases" JSON key and "fee_reductions" key
    - test_csv_attachment_columns: output DataFrame from a full pipeline run contains exactly the D-18 columns: key_sales_marketplace_sku, country, sales_region, sku, asin, week_start_date, avg_fee_per_unit, baseline_median_fee_per_unit, deviation_pct, direction, sustained_shift
    - test_csv_includes_sustained_shift_rows: anomaly_df with one sustained_shift=True row and one sustained_shift=False row — attach_csv_to_task() receives a DataFrame containing both rows; sustained row has sustained_shift=True
    - test_config_drives_task_id: load_config() reads CLICKUP_TASK_ID from audit_config.json; post_clickup_comment() is called with that task_id (mocked requests.post)
    - test_escalation_prompt_in_comment: the text returned by generate_narrative() (mocked) when passed to post_clickup_comment() results in a requests.post call where the JSON body comment_text ends with "Reply YES to this comment to open an investigation task for the top flagged SKUs."
  </behavior>
  <action>
    Create tests/test_detection.py. The file is intentionally RED on creation — all tests fail with ImportError because run_audit.py does not exist yet. Do NOT skip, xfail, or guard any test. The ImportError is the intended failure mode (TDD discipline, matching the pattern in tests/test_aggregation.py line 1-8 comment).

    File structure (mirror tests/test_aggregation.py exactly):
    1. Docstring header stating requirements covered (DETECT-02, DETECT-03, OUT-01, OUT-02, OUT-03, ESC-01) and that tests are intentionally RED until Wave 1 creates run_audit.py.
    2. Imports: pandas, pytest, unittest.mock (patch, MagicMock), datetime, pathlib. Import from run_audit: detect_anomalies, load_prior_counts, classify_sustained_shifts, append_to_history, build_anomaly_json, generate_narrative, post_clickup_comment, attach_csv_to_task, load_config.
    3. Four section banners (mirror test_aggregation.py comment style):
       - DETECT-02: Rolling baseline and anomaly threshold (3 tests)
       - DETECT-03: Consecutive count and sustained-shift classification (3 tests)
       - OUT-01 / ESC-01: Narrative generation and escalation prompt (2 tests)
       - OUT-02 / OUT-03: CSV attachment columns and config-driven task ID (3 tests)

    For DETECT-02 tests: use inline DataFrames with key_sales_marketplace_sku and week_start_date columns matching the sample_snapshot_df fixture pattern from PATTERNS.md conftest section. Weeks 1-7 fee ~3.0, week 8 fee 4.5 (47% deviation). Call detect_anomalies() directly with the DataFrame.

    For DETECT-03 tests: use tmp_path and monkeypatch.setattr("run_audit.HISTORY_PATH", tmp_path / "anomaly_history.csv") to isolate history CSV per test. Write a history CSV with run_date = datetime.date.today() - timedelta(days=7) for the consecutive_count_increments test (continuity passes). Write run_date = datetime.date.today() - timedelta(days=14) for the gap reset test (continuity fails, count resets to 0).

    For OUT-01/ESC-01 tests: mock anthropic.Anthropic using patch("anthropic.Anthropic"). The mock's .return_value.messages.create.return_value.content[0].text should be set to a string ending with the exact escalation prompt. Assert that the returned text from generate_narrative() contains "Reply YES to this comment to open an investigation task for the top flagged SKUs."

    For OUT-02/OUT-03 tests: use sample_anomaly_df fixture (defined in Task 2). For test_config_drives_task_id, write a temp audit_config.json with CLICKUP_TASK_ID="TEST_TASK_XYZ", call load_config(path=...) or monkeypatch the path, then mock requests.post and assert the POST URL contains "TEST_TASK_XYZ".

    Use inline assertion messages matching test_aggregation.py style: f"<function> must <expected>, got {actual}".
  </action>
  <verify>
    <automated>pytest tests/test_detection.py -x -q 2>&1 | head -20</automated>
  </verify>
  <done>Running pytest tests/test_detection.py produces "ImportError" (not "SyntaxError" or "collection error"). All 11 tests are collected and all fail with the same ImportError on the run_audit import. Phase 1 suite is unaffected: pytest tests/test_aggregation.py -x -q shows 9 passed.</done>
</task>

<task type="auto">
  <name>Task 2: Extend conftest.py, create audit_config.json, update .gitignore + requirements.txt, create snapshots/.gitkeep</name>
  <files>tests/conftest.py, audit_config.json, .gitignore, requirements.txt, snapshots/.gitkeep</files>
  <action>
    Four distinct edits; each is small and independent.

    1. EXTEND tests/conftest.py — append three fixtures at the bottom of the existing file without modifying the existing sample_pbi_rows and sample_sku_rows fixtures:

       a. sample_snapshot_df: imports pandas inside the fixture. Returns a DataFrame with columns key_sales_marketplace_sku (str), week_start_date (pd.Timestamp), avg_fee_per_unit (float). One SKU "US | US-OHFB-001" across 8 weeks (2026-01-05 to 2026-02-23, freq="7D"). Fee values: [3.0, 3.1, 2.9, 3.0, 3.05, 2.95, 3.0, 4.5] — week 8 is the spike.

       b. sample_audit_config: uses tmp_path. Creates a dict with THRESHOLD_PCT=15, CLICKUP_TASK_ID="TEST_TASK_123", RECIPIENTS=[], SUSTAINED_SHIFT_N=4. Writes it to tmp_path / "audit_config.json" via json.dumps. Returns (cfg_path, config) tuple.

       c. sample_anomaly_df: imports pandas and datetime inside the fixture. Returns a DataFrame with one row: key_sales_marketplace_sku="US | US-OHFB-001", country="US", sales_region="US", sku="US-OHFB-001", asin="B0EXAMPLE01", week_start_date=datetime.date(2026, 5, 26), avg_fee_per_unit=4.50, baseline_median_fee_per_unit=3.00, deviation_pct=50.0, direction="increase", sustained_shift=False.

    2. CREATE audit_config.json at project root with exact content:
       {"THRESHOLD_PCT": 15, "CLICKUP_TASK_ID": "PLACEHOLDER", "RECIPIENTS": [], "SUSTAINED_SHIFT_N": 4}
       (Pretty-printed with 4-space indent for readability.)

    3. UPDATE .gitignore — add two lines after the existing "output/*" and "!output/.gitkeep" lines:
       snapshots/*
       !snapshots/.gitkeep
       The *.csv rule already covers anomaly_history.csv. audit_config.json must NOT be added to .gitignore (it is intentionally git-tracked per D-08).

    4. UPDATE requirements.txt — append one line at the end: anthropic==0.101.0

    5. CREATE snapshots/.gitkeep as an empty file. This tracks the directory in git while .gitignore excludes snapshots/*.csv. The pattern is identical to the existing output/.gitkeep.
  </action>
  <verify>
    <automated>pytest tests/ -x -q</automated>
  </verify>
  <done>pytest tests/ -x -q shows: Phase 1 suite (9 tests) all pass. test_detection.py shows 11 ImportErrors (all fail with the same error — no collection failures). audit_config.json exists and contains THRESHOLD_PCT, CLICKUP_TASK_ID, RECIPIENTS, SUSTAINED_SHIFT_N. requirements.txt last line is anthropic==0.101.0. snapshots/.gitkeep is an empty file. .gitignore contains "snapshots/*" and "!snapshots/.gitkeep".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → run_audit module | Import boundary; does not exist yet — intentional RED state |
| audit_config.json → runtime config | File read at startup; contains only calibration values (no secrets) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | audit_config.json | mitigate | File contains ONLY calibration values (THRESHOLD_PCT, CLICKUP_TASK_ID, RECIPIENTS, SUSTAINED_SHIFT_N). If CLICKUP_API_KEY or ANTHROPIC_API_KEY ever appear in this file, raise SystemExit before any API call. Pre-commit grep check documented in plan notes. |
| T-02-02 | Tampering | snapshots/ directory | accept | Local filesystem; no external input. snapshots/*.csv are git-ignored to prevent accidental data commit. Low risk: internal tool, single user. |
| T-02-SC | Tampering | pip install anthropic==0.101.0 | mitigate | Package legitimacy audit completed in 02-RESEARCH.md (slopcheck OK, PyPI verified, Author: Anthropic). No blocking checkpoint required — package is first-party. |
</threat_model>

<verification>
After both tasks complete:
- pytest tests/test_detection.py -x -q → 11 ImportErrors (not SyntaxError or collection error)
- pytest tests/test_aggregation.py -x -q → 9 passed (Phase 1 regression clean)
- cat audit_config.json confirms THRESHOLD_PCT=15, CLICKUP_TASK_ID="PLACEHOLDER"
- tail -1 requirements.txt confirms anthropic==0.101.0
- grep -c "snapshots" .gitignore confirms 2 matches
</verification>

<success_criteria>
Wave 0 is complete when: all 11 tests in test_detection.py are collected by pytest and fail with ImportError (not with SyntaxError or "ERROR collecting"). All Phase 1 tests remain green. audit_config.json, snapshots/.gitkeep, and requirements.txt update are committed. The test file is the contract that Waves 1-3 implement against.
</success_criteria>

<output>
Create `.planning/phases/02-detection-output-pipeline/02-01-SUMMARY.md` when done.
</output>
