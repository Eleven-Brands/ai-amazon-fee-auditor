---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .env.example
  - requirements.txt
  - pytest.ini
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_aggregation.py
  - output/.gitkeep
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DETECT-01

must_haves:
  truths:
    - "Running `pytest tests/ -x -q` exits 0 with 8 tests collected and all passing"
    - "`.env` is not tracked by git; `.env.example` is committed"
    - "output/ directory exists and its contents are git-ignored"
    - "All 8 test functions in test_aggregation.py have concrete assertions, not just `pass`"
  artifacts:
    - path: ".gitignore"
      provides: "Credential and output file exclusion"
      contains: ".env"
    - path: ".env.example"
      provides: "Credential template"
      contains: "POWERBI_TENANT_ID="
    - path: "requirements.txt"
      provides: "Pinned dependency list"
      contains: "msal==1.36.0"
    - path: "pytest.ini"
      provides: "pytest configuration"
      contains: "testpaths = tests"
    - path: "tests/conftest.py"
      provides: "Shared fixtures for all tests"
      contains: "sample_pbi_rows"
    - path: "tests/test_aggregation.py"
      provides: "8 unit tests covering DATA-01, DATA-02, DETECT-01"
      contains: "test_weekly_avg_calculation"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_aggregation.py"
      via: "pytest fixture injection"
      pattern: "def sample_pbi_rows"
---

## Phase Goal

**As a** data engineer running the fee auditor, **I want to** have a working test harness and project scaffold before writing any production code, **so that** every function I write has automated verification from the first commit.

<objective>
Create the complete project scaffold — dependency manifest, credential template, git safety net, pytest configuration, and a full set of unit test stubs with concrete assertions. This plan has no production code; it exists so Wave 1 executors can run `pytest tests/ -x -q` to verify their work immediately.

Purpose: Establish Nyquist-compliant test coverage before writing a single line of explore_fees.py. All 8 tests from VALIDATION.md must be present and red (failing with ImportError or NotImplementedError) until Wave 2 implements the functions.

Output: .gitignore, .env.example, requirements.txt, pytest.ini, tests/__init__.py, tests/conftest.py, tests/test_aggregation.py, output/.gitkeep
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Project scaffold files (.gitignore, .env.example, requirements.txt, pytest.ini, output/)</name>
  <files>.gitignore, .env.example, requirements.txt, pytest.ini, output/.gitkeep</files>
  <read_first>
    .planning/phases/01-data-foundation/01-RESEARCH.md (Standard Stack section — version pins; Security Domain section — credential threat pattern)
    .planning/phases/01-data-foundation/01-CONTEXT.md (D-17, D-18 — no BigQuery; D-09 — env var names)
    CLAUDE.md (Critical Version Constraints section — library versions)
  </read_first>
  <action>
    Create five files. Exact contents:

    .gitignore: Entries must include `.env`, `output/`, `__pycache__/`, `*.pyc`, `*.csv`, `*.egg-info/`, `.pytest_cache/`, `dist/`. One entry per line. No blank lines between entries.

    .env.example: Three lines only — `POWERBI_TENANT_ID=`, `POWERBI_CLIENT_ID=`, `ANTHROPIC_API_KEY=` (placeholder for Phase 2, document with inline comment `# Phase 2`). Values are empty (no placeholder text after `=`). This file IS committed to git.

    requirements.txt: Pin all packages from RESEARCH.md Standard Stack section. Use `==` pins. Lines: `msal==1.36.0`, `requests==2.32.3`, `pandas==2.2.3`, `numpy==2.2.4`, `python-dotenv==1.0.1`, `pydantic==2.13.4`, `pytest==9.0.3`. No extras beyond what RESEARCH.md lists for Phase 1.

    pytest.ini: Minimal INI-format config. `[pytest]` section with two keys: `testpaths = tests` and `addopts = -q`. No other keys.

    output/.gitkeep: Empty file. The `output/` directory must exist in the repo but its contents are git-ignored (per .gitignore entry `output/`). The .gitkeep itself is NOT in `output/` path in .gitignore — only files inside `output/` are ignored. Correct .gitignore pattern: `output/*` with `!output/.gitkeep`.

    Security: Per D-09 and RESEARCH.md Security Domain — `.env` must appear in `.gitignore` before this plan is done. Verify with `git check-ignore .env` returning `.env` after creation.
  </action>
  <verify>
    <automated>python -c "import os; assert os.path.exists('.gitignore'); assert os.path.exists('.env.example'); assert os.path.exists('requirements.txt'); assert os.path.exists('pytest.ini'); assert os.path.exists('output/.gitkeep'); open('.gitignore').read().count('.env') >= 1 and print('OK')"</automated>
  </verify>
  <done>
    All five files exist. `.gitignore` contains `.env`. `requirements.txt` contains `msal==1.36.0`. `pytest.ini` contains `testpaths = tests`. `output/.gitkeep` is tracked by git but `output/*.csv` is ignored.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test fixtures (conftest.py) and 8 unit tests (test_aggregation.py)</name>
  <files>tests/__init__.py, tests/conftest.py, tests/test_aggregation.py</files>
  <read_first>
    .planning/phases/01-data-foundation/01-VALIDATION.md (Per-Task Verification Map — all 8 test IDs and their behavior descriptions)
    .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 3 — DAX column name format; Pattern 4 — pandas ISO week; Pattern 5 — SKU join; Pitfall 5 — column name mangling; Pitfall 7 — amzn.gr.* join noise; full auth+query code example)
    .planning/phases/01-data-foundation/01-CONTEXT.md (D-03 — fee metric; D-04 — weekly avg; D-05 — include amzn.gr.*; D-06 — join key; D-11 — required CSV columns; D-15 — country vs sales region)
  </read_first>
  <behavior>
    All 8 tests must be RED (fail or raise ImportError) before explore_fees.py exists, and GREEN after Wave 2 implements the functions. Write tests first, then functions in explore_fees.py (Wave 2).

    - test_run_dax_returns_expected_shape: mock `requests.post` to return a valid PBI JSON response with rows containing `fact_fee_preview[key_sales_marketplace_sku]`, `[YEAR]`, `[WEEKNUM]`, `[avg_fee_per_unit]` keys. Assert `run_dax()` returns a list of dicts and len > 0.
    - test_weekly_avg_calculation: given a conftest fixture with two rows for the same key in the same week (fee 1.50 and 2.50), call `process_pbi_rows()` and assert `avg_fee_per_unit` column exists with value 2.00 for that key+week (average of 1.50 and 2.50). Note: the DAX query already does AVERAGE aggregation, so this test validates column renaming + numeric type, not re-aggregation.
    - test_value_count_within_limit: call `validate_value_count(n_keys=5274, n_weeks=16, n_cols=5)` and assert result < 1_000_000. Also assert `validate_value_count(n_keys=10000, n_weeks=16, n_cols=10)` raises ValueError.
    - test_country_extraction: given a DataFrame with `key_sales_marketplace_sku` values `["US | US-OHFB-001", "DE | DE-OHFB-002", "GB | GB-OHFB-003"]`, call `extract_country()` and assert country column contains `["US", "DE", "GB"]`.
    - test_currency_mapping_completeness: call `get_currency_for_country()` with each of `["US", "CA", "GB", "MX", "DE", "FR", "ES", "IT"]` and assert none return "UNKNOWN". Also assert `get_currency_for_country("ZZ")` returns "UNKNOWN".
    - test_amzn_gr_keys_preserved: given a merged DataFrame with 3 rows where one key starts with `amzn.gr.`, assert the output contains all 3 rows (no rows dropped), and the `amzn.gr.*` row has NaN for `asin` and `sku` columns.
    - test_csv_schema_columns: call `build_output_df()` with sample data and assert all D-11 columns are present: `key_sales_marketplace_sku`, `country`, `sales_region`, `sku`, `asin`, `week_start_date`, `avg_fee_per_unit`, `currency`.
    - test_week_start_is_monday: call `iso_to_week_start(2026, 1)` and assert result.weekday() == 0 (Monday). Also assert `iso_to_week_start(2025, 53)` does not raise an exception (year-boundary edge case).
  </behavior>
  <action>
    Create `tests/__init__.py` as an empty file.

    Create `tests/conftest.py` with two pytest fixtures:

    Fixture `sample_pbi_rows`: returns a list of 4 dicts mimicking the Power BI executeQueries JSON response format. Column keys must use the PBI fully-qualified format: `"fact_fee_preview[key_sales_marketplace_sku]"`, `"[YEAR]"`, `"[WEEKNUM]"`, `"[avg_fee_per_unit]"`. Include two keys: `"US | US-OHFB-001"` and `"amzn.gr.ABCD1234"`. Use ISO year 2026, weeks 1 and 2.

    Fixture `sample_sku_rows`: returns a list of dicts mimicking the SKUs DAX response with keys `"SKUs[Key Column: Country | SKU]"`, `"SKUs[SKU]"`, `"SKUs[ASIN]"`, `"SKUs[Sales Region]"`. Include one row for `"US | US-OHFB-001"` with SKU=`"US-OHFB-001"`, ASIN=`"B0EXAMPLE01"`, Sales Region=`"US"`.

    Create `tests/test_aggregation.py` with all 8 test functions from the behavior block above. Each function imports from `explore_fees` (which does not exist yet — the import will fail with ImportError until Wave 2 creates it). Use `unittest.mock.patch` for `test_run_dax_returns_expected_shape`. Do not use `@pytest.mark.skip` — the tests must be naturally red, not skipped.

    Function signatures to import from explore_fees (must match exactly what Wave 2 will implement):
    - `run_dax(dax: str, token: str) -> list[dict]`
    - `process_pbi_rows(rows: list[dict]) -> pd.DataFrame`
    - `validate_value_count(n_keys: int, n_weeks: int, n_cols: int) -> int`
    - `extract_country(df: pd.DataFrame) -> pd.DataFrame`
    - `get_currency_for_country(country: str) -> str`
    - `build_output_df(fee_df: pd.DataFrame, sku_df: pd.DataFrame) -> pd.DataFrame`
    - `iso_to_week_start(iso_year: int, iso_week: int) -> pd.Timestamp`
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q 2>&1 | grep -E "(ERROR|ImportError|collected|passed|failed)" | head -5</automated>
  </verify>
  <done>
    `tests/__init__.py`, `tests/conftest.py`, and `tests/test_aggregation.py` exist. Running `pytest tests/ -x -q` shows 8 tests collected and all failing with ImportError (because explore_fees.py does not exist yet). Zero skipped tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repo | .env file must never cross into git history |
| developer → filesystem | token cache file at ~/.claude/ must not be readable by other OS users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` file | mitigate | `.gitignore` entry for `.env` created in Task 1; verified with `git check-ignore .env` |
| T-01-02 | Information Disclosure | `~/.claude/powerbi_token_cache.json` | accept (Wave 0 scope) | Token cache is outside this plan's file scope; file permission check is a manual verification item in VALIDATION.md; addressed in PLAN 02 |
| T-01-SC | Tampering | pip install from requirements.txt | accept | All 7 packages scored [OK] by slopcheck 0.6.1 (RESEARCH.md Package Legitimacy Audit); no [ASSUMED] or [SUS] packages present |
</threat_model>

<verification>
1. `pytest tests/ -x -q` — 8 collected, 8 failing with ImportError (explore_fees.py does not exist)
2. `git check-ignore .env` — prints `.env`
3. `git check-ignore output/explore_fees_20260527.csv` — prints the path (output/ contents are ignored)
4. `cat requirements.txt | grep msal` — prints `msal==1.36.0`
5. `cat pytest.ini | grep testpaths` — prints `testpaths = tests`
</verification>

<success_criteria>
- 8 tests exist in tests/test_aggregation.py, all naturally red (ImportError)
- .gitignore excludes .env and output/ contents
- .env.example contains POWERBI_TENANT_ID= and POWERBI_CLIENT_ID= with empty values
- requirements.txt lists 7 packages with == pins matching RESEARCH.md versions
- pytest.ini configured with testpaths = tests
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-01-SUMMARY.md` when done
</output>
